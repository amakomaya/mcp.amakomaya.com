# ==========================================
# Amakomaya MCP Server - Environment Config
# ==========================================

# --- Keycloak (Authentication) ---
KEYCLOAK_URL=https://auth.amakomaya.com
KEYCLOAK_REALM=amakomaya
KEYCLOAK_CLIENT_ID=amakomaya-mcp
KEYCLOAK_CLIENT_SECRET=change-me
# Public redirect URI registered in Keycloak for this MCP server
KEYCLOAK_REDIRECT_URI=http://localhost:8000/auth/callback

# --- FHIR Server ---
FHIR_BASE_URL=https://api.amakomaya.com
FHIR_TIMEOUT=30
# Service-account bearer token used for the Patient lookup step only
# (never used to fetch clinical data - all clinical reads are patient-scoped)
FHIR_SERVICE_TOKEN=

# --- Server ---
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# --- Session ---
# Secret used to sign the server-side session cookie/id (not the JWT itself)
SESSION_SECRET_KEY=change-me-in-production
SESSION_TTL_SECONDS=3600

# --- Auth toggle (temporary) ---
# TODO(auth): keep this true everywhere except local protocol testing.
# When false, ALL authentication (Keycloak, JWT, session) is bypassed and
# every request is served as AUTH_DISABLED_PATIENT_ID. Never set false
# where ENVIRONMENT=production - the server refuses to start.
AUTH_ENABLED=true
# FHIR Patient resource id used for every request while AUTH_ENABLED=false.
AUTH_DISABLED_PATIENT_ID=
ENVIRONMENT=development
